#include "main.h"

/**
 * _strcpy - function that copy
 * @dest: string
 * @src: string source
 * Return: void
 */

char *_strcpy(char *dest, char *src)
{
	char *c;

	c = strcpy(dest, src);
	return (c);
}
